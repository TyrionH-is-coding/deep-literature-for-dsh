# V02-003B 引擎候选交付（待总控复核）

2026-09-17，phase=review。工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b`，分支 `codex/v02-003b-engine-candidate`。基线 `4e5e6b964ddb8b823be17fb61774f8386bb0ba98`；候选来源提交 `ca02db6beb2cf45ad0fd69618313971d0a346c55`。报告提交与来源提交分开，最终报告 SHA 见交接。

仅修改 npm 自身版本为 `0.2.0-dev.1`、Python 自身版本为 `0.2.0.dev1`，增加专属验收脚本和证据；没有生产逻辑、依赖、schema 或 dataFormat 变化。构建器产生的 `lib/client.js` 仅换行变化，经内容比较后恢复原跟踪字节；不提交该文件。

## 本轮实际验证

独立 `.venv`，Python 3.11.9、Node 24.18.0、npm 11.16.0；npm 使用 `ci --ignore-scripts --legacy-peer-deps --no-audit --no-fund`，Python 按 002 的 `docs/codex-v02/evidence/audit-python-requirements.txt` 精确安装。环境与全部 freeze 在证据目录。

- `node scripts/v02-003b-run.mjs build`：通过，wheel、TypeScript、client 均构建成功。
- `node scripts/v02-003b-run.mjs python`：515 passed，3 skipped，148.80 秒。跳过 Windows 不可创建目录 symlink、Unix zombie lifecycle、未启用真实 OS credential service；完整名称/原因见 environment.json 和 python-junit.xml。
- `node scripts/v02-003b-run.mjs offline`：默认完整 41 条离线命令通过，包含 navigation-contract、Excel 动作、wheel 源码字节检查及 CLI 安装测试。
- `node scripts/v02-003b-run.mjs assets`：4 条资产链通过。
- `node scripts/v02-003b-run.mjs pack`：npm pack 成功，54 个文件。
- `.venv/Scripts/python.exe scripts/v02-003b-artifact.py`：tgz 中唯一 wheel 与 dist wheel 字节一致，51 个 Python 文件与候选源码字节完全相同，npm/Python 版本核验通过。

每条测试的实际时间、退出码和命令在 `V02-003B-evidence/runs.json`，原始 stdout/stderr 保留。新增 artifact 脚本首次错误地要求 METADATA 以 LF 结尾，遇到正常 CRLF 导致断言失败；改用标准 email 元数据解析后通过，未修改制品或产品。

## 制品

- tgz：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b/dsh-external-dsh-scientific-reading-0.2.0-dev.1.tgz`
- SHA256：`fdce0ada390e2f27615c6b6050dbf7ef83c871d6a6304c4235e4414f667c8930`
- wheel：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b/dist/python/dsh_scientific_reading_engine-0.2.0.dev1-py3-none-any.whl`
- SHA256：`cd6120c31dec1c096ca038ad3775e0456c6c9c45898fb630b3e2a5a9e3e88566`

没有发布、上传或打 tag。B pins 和本地 archive 已绑定上述来源；B runtime 锁只变插件 version/integrity。

## 阻塞与未验证

B 干净候选来源提交 `7a04cf1287f0f4200e736e91618a6b7956949ff2` 的现有 package-release 流程在 module-graph.mjs:143 报 `engine_pin_mismatch`。任务允许更新 pins，却未允许同步 `src/modules/catalog.json` 四个重复引擎身份字段。未放宽门禁，未修改禁止路径。

因此没有 B ZIP、解包安装、安装产物 F1/worker/前端复验或主流程 smoke，不以 A 源码/离线测试替代这些验收。真实模型、MinerU、用户数据、Office/WPS、跨平台均未测试；F1 是否关闭由总控决定。未启动任何工作台实例，测试进程均已正常结束。

最小追加建议：只授权同步 B catalog 的 `modules[id=engine].version` 及 `externalEngine.version/sourceCommit/sha256` 到 pins，保持校验器与生产逻辑不变，然后重新提交 B 来源、执行模块测试、打包、解包安装和本卡剩余验收。A 制品可沿用，但必须继续核对本报告摘要。
