# V02-004B：解析提交前真实中断的安全续接

状态：review。004A-F1 的源码修复与合成进程验证完成，等待总控独立制品复验；本卡未制作发行包。

## 身份与修改

- A 工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004b`，分支 `codex/v02-004b-parse-recovery`，开工 clean，base `dcdb8e6cb23dd8329ceabd4487db0a79fae470ea`。
- B 只读 context `ceea8aebc90f99e0daad9344e9a38d2cca4d7658`；通过 git show 读取任务卡、AGENTS、session-protocol、004A-control-review。A 无 AGENTS。未使用应用默认 dsh reader 源码，也未改 B 台账或 004C 目录。
- 实现提交 `f9dbab5dc13accf0b78db48151dc46e6a3acd808`。后续提交仅加入本报告、receipt、测试证据。receipt 的 ownerSession=null，没有用总控 ID 冒充自身 ID。
- 生产仅 `reading_pipeline.py`、`mineru_service.py`，共 26 行增加、6 行删除。增加可选的 running `result.source_sha256`，没有 schema、版本、依赖、CLI、worker、UI、取消协议、阶段顺序或公共签名变化。

## 安全识别与保护

旧格式例外只用于 `paper_parse_upgrade`，且必须同时满足：不要求 completed；nested stage 为 running；来源字段完全缺失（显式 null 也拒绝）；job 为 mineru_running；generation 原有 PDF 存在；completed pdf_acquisition 登记全 SHA 一致；不存在正式 parsed/mineru 目录和资产 manifest。原有 metadata 身份、generation paper_id、base PDF 与 generation PDF 的全 SHA 检查保持生效。

只有尚未提交的解析可进入服务重试。已有来源字段但不一致、completed 缺失来源、其他状态/阶段、缺原 PDF、错误 PDF 登记、身份错配、正式输出或清单都不通过兼容例外。缺失 PDF 的复制移到阶段校验之后，避免第一次拒绝却补齐证据、第二次被放行。负例均重复尝试两次。

新 running checkpoint 明确记录 SHA；旧记录通过上述证据识别，不需要删除 generation。服务仍持有 MinerU 发布锁、检查 acquisition、验证缓存 identity 和完整性；require_stage 仍只接受 completed。scope/publication_guard、失败任务必须显式续接、已保存 batch 和 Reader 发布行为均未修改。

## 证据与实际命令

证据目录：[V02-004B-evidence](V02-004B-evidence/)。Windows；独立 `.venv`，Python 3.11.9、Node 24.18.0。Python 使用 `docs/codex-v02/evidence/audit-python-requirements.txt` 精确版本，freeze 已保存。Node 使用原 package-lock，通过 `npm ci --legacy-peer-deps` 安装，不改锁文件。

1. 原样运行 `.venv/Scripts/python.exe -m pytest -q engine/tests/test_v02_pipeline_contract.py::test_cross_process_stage_recovery[parse-kill-before] --junitxml=docs/codex-v02/V02-004B-evidence/baseline.xml`：**1 failed / exit 1**，completed 断言保留，错误为 generation_workspace_conflict。原 004A 探针及九个测试文件完全未改。
2. 修改前运行 `.venv/Scripts/python.exe scripts/v02-004b-recovery.py baseline C:/tmp/v02-004b-old`：真实 worker 被终止，原探针继续后留下 failed 库，legacy-baseline.json 保存全状态。修改后运行同脚本 `resume C:/tmp/v02-004b-old`：**exit 0，completed**。新进程显式 `{}` 续接，parent 不变，旧无 SHA running checkpoint、进程身份、最终资产 SHA 均在 legacy-resume.json；无 claim owner 文件或存活登记进程。该次恢复在最终新增 manifest 存在性拒绝条件之前完成；最终 23 例包含同一旧格式正例及 manifest 拒绝例。
3. `.venv/Scripts/python.exe scripts/v02-004a-probe.py --output C:/tmp/v02-004b-new`：**六场景全部 completed / exit 0**。解析前失败/终止允许 provider 重试；解析后终止复用正式缓存；翻译保存后失败/终止保留 batch；baseline 完成。parent、PID/claim 回收、资产保护和衍生进程证据见 processes 各场景 result.json/calls.jsonl/worker 日志。该组也在最终 manifest 拒绝条件增加之前运行，最终源码的真实进程覆盖由下面完整 pytest 集合提供。
4. 最终定向 Python 命令使用 `-q -rs` 与 `--junitxml=docs/codex-v02/V02-004B-evidence/final-python.xml`，包含下列全部文件：**136 passed / 3 skipped，exit 0，131.21 秒**，见 final-python.log/xml。新增 23 个恢复/负例和原 004A 九项均包含在内，不把重复试跑计数相加。

```text
.venv/Scripts/python.exe -m pytest -q -rs
engine/tests/test_v02_parse_recovery.py engine/tests/test_v02_pipeline_contract.py
engine/tests/test_reading_pipeline_integrity.py engine/tests/test_worker_full_read.py
engine/tests/test_background_launcher.py engine/tests/test_pid_liveness.py
engine/tests/test_foreground.py engine/tests/test_mineru_parse_gate.py
engine/tests/test_mineru_resume_contract.py engine/tests/test_pipeline_excel_end_to_end.py
engine/tests/test_full_read_service.py engine/tests/test_full_read_renderer.py
engine/tests/test_candidate_rebuild.py engine/tests/test_platform_integration.py
engine/tests/test_subprocess_utils.py engine/tests/test_data_guard_services.py
engine/tests/test_data_guard.py engine/tests/test_data_guard_assets.py
engine/tests/test_scope_boundary.py engine/tests/test_mineru_artifacts.py
engine/tests/test_generation_package_manifest.py
--junitxml=docs/codex-v02/V02-004B-evidence/final-python.xml
```

5. `npx tsc -p tsconfig.json`、`node scripts/build-client.mjs` 均 exit 0；`node tests/{full-read-pipeline,reading-routes,full-read-routes,restart-recovery-pid-safety,task7-hidden-subprocess}.mjs` 五条独立命令均 exit 0。生成的 tracked lib/client.js 已还原。没有执行包含 wheel 打包的 npm build；验证限编译/客户端构建，符合不制作发行包的范围。

试跑如实保留：首次 pytest 在依赖安装结束前启动，因无 pytest 退出，见 baseline-env-not-ready；普通 npm ci 因既有 peer 冲突失败，后以 legacy-peer-deps 成功；recovery 首轮 30 pass / 1 fail 为负例夹具调用 save_job 时就被目录身份守卫拒绝，已改成写入损坏的磁盘 checkpoint 后测试读取边界。随后 recovery-final 31 pass，guards-final 23 pass，最终完整集合另列，不混算。regression 早期集合 104 pass / 3 skip。没有隐藏新失败。

## 清理、限制与回退

所有 provider 和论文为本地合成夹具，进程 worker 拒绝 Python 网络连接；未调用真实模型/MinerU/凭据/用户安装。各 worker 隐藏启动并记录 PID/创建身份，清理只针对本任务登记进程。旧库与独立六场景库保留在上述 C:/tmp 专属路径，供复核；pytest 临时库自动清理，没有删除 generation 恢复。清理审计见 process-cleanup.json。

未验证真实模型/科学内容质量、发行制品 Python 3.11.16 / Node 22、安装升级、多 OS、全部并发/崩溃窗口或端到端取消。三个 skip：Unix zombie、未解锁 OS credential service、Windows 目录 symlink 无权限。scope 定向单测通过不等于完整授权矩阵或真实进程 scope 组合已验收。

旧 running 若已有正式目录或 manifest 仍保守拒绝，超出“提交前中断”的兼容范围。任一全 SHA/身份/缓存/已确认产物保护回归均应撤回实现提交并重新调查；回退不会迁移 schema 或删除数据，新可选字段对旧读者无格式版本要求。仅代码回退会重新暴露旧 checkpoint 的 F1，不能将其称作数据恢复。没有合并 main、tag、发布、创建后继任务或更改用户实例。
