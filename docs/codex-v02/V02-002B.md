# V02-002B：恢复导航合同测试的有效覆盖

状态：review；本卡验收项通过，待总控独立验收。

## 身份与范围

- 工作树：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-002b`。
- 分支：`codex/v02-002b-navigation-contract`。
- 完整源码基线：`d26cb9e88d0f7884db8e69a5a2d18286a62aaf83`。
- 工作台固定 contextCommit：`1b7729d4cff68b398e626a4527727e5775b4d50e`。
- 开工确认 HEAD、分支和目录一致，`git status --short` 为空。origin 仍登记 `https://github.com/TyrionH-is-coding/dsh-scientific-reading.git`；本卡不 fetch、不更新固定基线。
- 基线核对后立即写入 [运行回执](V02-002B-receipt.json)。无法可靠获取本任务真实 session ID，ownerSession=null。
- 使用 `git show <contextCommit>:<path>` 读取工作台 AGENTS、任务卡、002 review、`docs/project/auto-loop.md` 和 session-protocol。首次尝试根路径 `auto-loop.md` 返回不存在，随后按 AGENTS 引用读取上述正确路径。工作台模块命令不适用于这个独立引擎仓库，本卡不在工作台执行或写入模块登记。
- 生产实现、接口、数据/schema、依赖、锁文件、版本和 Excel 测试均未修改；未读取 003A 可变工作树。变更限于导航测试两条期望、package.json 的 test:offline，以及本卡资料。

## 失败归因与最小修复

[before.log](V02-002B-evidence/before.log) 保存本树构建后的原始复现，exit=1，首个深度比较失败于 `tests/navigation-contract.mjs:69`，与 002 记录一致。

| 期望补全 | 现行合同证据 | 判断 |
| --- | --- | --- |
| 两条 item 增加 `journal: ''` | `src/routes.ts:179` 对缺失字段调用 safeString；默认 `tests/library-navigation-api.mjs` 已精确断言该字段 | 夹具未提供 journal，空字符串是既有返回合同 |
| 两条 item 增加 `search_matches: []` | `src/routes.ts:188-189` 在查询模式加入 safeSearchMatches；`:310` 以非空 query 控制；原请求含 `q=cell` | 夹具缺失匹配数据，空数组是既有查询返回合同 |

仍使用完整 `assert.deepEqual`，没有剔除实际返回字段或改成宽松匹配。默认导航 API 测试另外检查非查询响应不含 search_matches，并检查合法命中与不安全 conclusion ID 的筛除；该测试未修改。

[after.log](V02-002B-evidence/after.log) 保存仅补全上述期望后的完整独立执行，exit=0。首个断言之后没有新失败，不需要第二轮期望更新，也没有发现需要生产修复或无法解释的合同差异。

后续原断言完整执行，包括：列表脱敏与 CLI 参数映射；非法分页及方法拒绝；详情安全字段保留/敏感字段移除；摘要类型归一化；PDF 内容、Content-Type 和活动代次解析调用；动作及方法保护；路径穿越/多余段拒绝；作业/继续结果的嵌套脱敏；引擎失败响应脱敏；超大或非法请求 body 不得调用动作引擎。

package.json 的 test:offline 在 `library-navigation-api.mjs` 后加入 `navigation-contract.mjs`。原默认链全部保留；通过已有 `npm test` 编排，该检查成为默认离线覆盖的一部分。Reader、资产与工具返回相关的既有默认测试未改动。

## 环境与重放

Windows、Node 24.18.0、npm 11.16.0、Python 3.11.9、TypeScript 5.9.3。见 [environment.json](V02-002B-evidence/environment.json)、[实际 Python freeze](V02-002B-evidence/python-freeze.txt)。依赖和构建输出位于本工作树 node_modules、.venv、lib、dist；没有共享或复制其他 worktree 的 venv。测试用 mkdtemp 等合成临时夹具，不使用用户库、实例、凭据或真实模型/MinerU 服务。

```powershell
npm ci --ignore-scripts --legacy-peer-deps
python -m venv .venv
.venv/Scripts/python.exe -m pip install -r docs/codex-v02/evidence/audit-python-requirements.txt
node docs/codex-v02/V02-002B-evidence/run.mjs build
node docs/codex-v02/V02-002B-evidence/run.mjs before
# 仅在基线测试尚未补全期望时，before 才预期失败；交付版重放将通过。
node docs/codex-v02/V02-002B-evidence/run.mjs after
node docs/codex-v02/V02-002B-evidence/run.mjs offline
node docs/codex-v02/V02-002B-evidence/run.mjs assets
```

[运行器](V02-002B-evidence/run.mjs) 显式使用本树 Python，启用 UTF-8、禁用原生 keyring 测试，并将固定审计依赖清单通过 PIP_CONSTRAINT 传给构建。日志保留命令、stdout/stderr；[test-runs.json](V02-002B-evidence/test-runs.json) 记录开始/结束时间和真实退出码。重放会追加结果并覆盖同名日志，审计原始结果应从交付提交读取。依赖安装见 [npm-ci.log](V02-002B-evidence/npm-ci.log) 和 [python-install.log](V02-002B-evidence/python-install.log)，两项 exit=0。

## 验收结果

| 执行项 | 本次实际结果 | 原始证据 |
| --- | --- | --- |
| build:ci | exit=0；本树构建 wheel、TypeScript 和客户端完成 | [build.log](V02-002B-evidence/build.log) |
| 修改前独立导航测试 | exit=1；首个期望缺失 journal/search_matches | [before.log](V02-002B-evidence/before.log) |
| 修改后完整导航测试 | exit=0；全部原有后续断言执行完成 | [after.log](V02-002B-evidence/after.log) |
| 默认 test:offline | exit=0；41 条命令全部执行通过，包含新增导航测试 | [offline.log](V02-002B-evidence/offline.log) |
| 默认 test:assets | exit=0；4 条命令全部执行通过 | [assets.log](V02-002B-evidence/assets.log) |
| 变更范围/断言保全检查 | exit=0；package 仅新增该默认测试，导航测试仅补两条完整期望，其他断言逐字保留；生产及锁文件未变 | [scope-check.json](V02-002B-evidence/scope-check.json)、[检查脚本](V02-002B-evidence/check-scope.py) |
| Git 差异检查 | `git diff --check` exit=0 | Git 仅提示工作树 LF 将规范化为 CRLF，无空白错误 |

默认 Node 链由原 44 条变为 45 条（41 offline + 4 assets），此次均通过；计数单位为命令，不是测试 case 或覆盖率。通过完整 npm 链的 `&&` 编排和结束状态核验，没有首失败截断导致的未执行后续命令。离线链出现 Node DEP0190（shell 参数拼接）弃用警告，未导致失败；原始 stderr 完整保留，未在本卡越界修改相关实现。

本卡没有未解决的导航合同差异或生产缺陷。F3 的测试恢复与默认链纳入已交付，是否关闭由总控决定；不据此关闭 F1/F2 或宣称产品全部测试通过。

## 限制与恢复

未执行完整 Python pytest 或总 `npm test`；本卡执行必要 build 和完整默认 Node 链，Python 仅支撑 Node 本地夹具。没有借用 002 的 Python 通过记录作为本卡结果。Node 22、其他 OS/架构、真实模型/MinerU、用户论文及真实使用验收不在本卡已验证范围；默认验收脚本的 `--self-check` 不等于真实使用验收。F1/F2 不属于本卡修复范围。

回退只需撤销本卡测试与脚本变更，无生产接口或数据迁移回退。交付保持 review；未合并 main、发布、更新总控台账或创建后继任务。

交付环境记录：首次 git commit 因未配置作者身份失败，未生成提交；随后按基线已使用的 `Codex <codex@openai.com>` 通过单次 `git -c user.name=Codex -c user.email=codex@openai.com commit` 提交，不修改全局或共享仓库配置。构建曾将受管 lib/client.js 写为 LF，Git 规范化后无内容差异；提交前已恢复该生成文件的基线检出字节。
