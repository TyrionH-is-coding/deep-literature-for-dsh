# V02-004D：安装恢复候选复验

待总控 review，未发布。工作树 C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004d，分支 codex/v02-004d-installed-recovery；base bf09c557bcedbba1d843579401d73b7beb65705d，B context 11581f7f2b0530a11179f0b25b90de4cb6559f1d。应用默认 dsh reader 未使用。

## 制品与范围

A clean 构建/pack 来源 a45aff9a6d5419c1b7a5cbc7954355176d56fac5。npm 0.2.0-dev.3、Python 0.2.0.dev2；tgz SHA256 a2e9e629140688df3fce947c4118e32157326a7e0a75c3d7952f6a67b413ba44；wheel SHA256 37f3da559ba74f022f4b73c3757b55299254ee50a75f04af49162b00749f4f83。报告另提交，不重建归档。

仅版本与专属探针/证据；未改生产算法、原测试、客户端、schema 或依赖，未纳入004E。正常 build:ci/npm pack 从 clean tracked 源执行，runs.json 留证。artifact.json 核对全部 51 个 Python 文件与源码字节，完整 SHA 列表及客户端当前构建 hash 均保存。独立 Node 22.22.2/Python 3.11.16 二进制与 pins 归档匹配，见 build-runtime.json；.venv 按原 audit-python-requirements.txt 安装，freeze/安装日志保留。

## 本轮实际结果

- 完整 Python：547 passed / 3 skipped，exit 0，505.41秒。跳过 Windows symlink 权限、Unix zombie、未开启原生凭据服务。测试提交 deec00afb4c2c00cded9d648f6a79c4be6ec9968 与制品来源之间生产/原测试/依赖零差异，仅专属探针完善，见 test-source-equivalence.json。
- 默认 Node offline 42 条与 assets 4 条命令、build/pack 全通过。原 bundled-engine 的 PYTHONPATH 测试仅计源码回归，不当作安装证明。
- 安装版 32 项断言全通过：23 恢复正反例＋原004A九项。六场景全部 completed/schema4，同parent、PID/claim回收、解析提交前provider两次/其他一次、已保存批次/解析资产不变；不完整新代保留正式Reader字节、mtime与路径，允许输入变更后stale。取消边界断言仅证实缺口仍存在。
- 安装解释器是本卡v4do新release的私有venv；每个探针worker用 -I -X utf8，保存executable/sys.path/engine/reader/version及逐文件SHA。无checkout PYTHONPATH/sys.path覆盖。标准库适配器运行原断言，不安装额外测试依赖、不更改断言。仅provider/翻译与故障边界是合成夹具；default runner、真实服务、持久状态、worker、发布和derived进程均来自wheel。

## 旧记录来源与恢复

本卡独立v4do由已校验dev.2 ZIP安装（Python dev1）；旧安装进程在provider内被真实终止，形成无source_sha256的running checkpoint，旧进程后续复现generation_workspace_conflict。保存parent/进程/状态及原JSON/hash。经本卡验证解包安装器原地升级到dev.3，同root/parent/原checkpoint在新安装解释器显式续接completed，provider总两次，owner.json清空、登记进程均退出。

首轮legacy/保存原JSON内容和hash；为补齐原始文件副本，再以本卡保留的旧release解释器产生独立legacy-raw，随后用新release受控续接。legacy-raw/legacy-original-files保存可读原文件；original-files.zip保留不受Git换行归一化影响的原始字节，逐文件hash核对一致，original-files-archive.json给出ZIP/hash。后者是增强原始字节归档的独立重复，不和首轮合并计数。未删除generation或改写checkpoint促成恢复，未修改前任务实例。

## 失败与限制

early-attempts.json记录依赖尚未完成时缺pytest、缺git作者身份、客户端纯CRLF/LF索引状态触发clean门禁，以及pack与offline重建wheel重叠导致缺wheel候选被来源校验拒绝。最终候选串行重建/打包后才交B使用，没有放宽门禁。汇总脚本初次默认编码读取中文JSON失败，改显式UTF-8后成功，没有影响产品或测试。

B同一制品的ZIP/安装字节、F1 Excel、Reader HTTP、合成主流程和重启证据见B docs/project/evidence/V02-004D。仅本地合成论文/provider；未使用真实模型、MinerU、凭据或用户库。未验证Office/WPS、非Windows、真实科学质量、全部崩溃窗口；客户端未变，不重复浏览器视觉，不从API声称新UI能力。取消/004A-G1和F2仍开放。

所有本卡实例/worker最终清理见B cleanup.json；合成状态和制品保留供复核。回退源码不回退数据，旧引擎会重新暴露旧running的F1。未发布、tag、合入main/总控、写台账或创建后继。
