# V02-003D 引擎客户端候选

范围：A client/client.js 与原构建器生成的 lib/client.js；npm 自身版本 0.2.0-dev.2，Python 分模块保留 0.2.0.dev1。基线 e3f58d1710413b5c6340fe55974a227b97ee8373；contextCommit 7e0a3e4ffbddf21cf5471d0ef14e57411fd1565f。

新增 renderXlsxPending 通过 el/textContent 在详情抽屉显示暂停原因、文献 ID、中文个人字段及备份核对建议。同批其他文献单独标明；缺少细节和未知 xlsx reason 保守显示，不暴露原始错误/个人值。正常任务和 PDF gate 保持原路径。未新增写入、自动续跑或解决冲突操作，接口/schema 不变。

新增 tests/client-xlsx-conflicts.mjs 执行真实 renderDrawer 函数并断言生成的节点树，10 组检查，接入默认 test:offline。003C HTTP 形状在修复前失败，保留 V02-003D-before.txt。该 Node 测试使用最小 DOM 实现；安装浏览器验收单独在 B 证据记录，不能互相代替。

构建在本工作树独立 .venv，使用 002 固定 Python requirements 与既有 npm lock。build/offline/assets/freeze 的执行结果保存在 V02-003D-evidence；完整 Python 套件本卡不重复执行，须核对 git diff engine 为空及制品 Python 字节对应。

来源提交 `26abee5f8c8e4f92f26a9b7fba4ddd236e523b05`。实际 build、42 条默认离线命令（含新增 10 组 DOM 渲染检查）、4 条资产链、pack 全通过。npm pack 54 文件；tgz SHA256 `2e1ec6ebbc9ca37d33060d978f5d1f855455bb4423cc5b481957b3da0259bc09`，wheel SHA256 `b5d6439b1e3e895e0127dac27ec2b12b9f0bdd46d813896a6a90fd990e2b66d3`；tgz 客户端字节与 lib/client.js 一致，51 个 Python 文件与源码一致。环境 Node 24.18.0 / Python 3.11.9；B 安装按固定 Node 22.22.2 / Python 3.11.16 另验。

A 未启动持久宿主。B 的最终安装结果与进程清理见 B docs/project/evidence/V02-003D.md。
